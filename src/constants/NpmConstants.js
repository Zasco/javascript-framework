import { npmModule } from 'javascript-framework';

/**
 * @since {NEXT_VERSION}
 * @see {@link npmModule.Types.VersionBumpTypes}
 * @readonly
 * @type {npmModule.Types.VersionBumpTypes}
 */
export const VERSION_BUMP_TYPES = Object.freeze(['major', 'minor', 'patch']);