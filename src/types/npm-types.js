/**
 * @since {NEXT_VERSION}
 * @typedef {readonly ['major', 'minor', 'patch']} VersionBumpTypes The allowed version bump types for the `npm version` command.
 * @typedef {VersionBumpTypes[number]} VersionBumpType A single allowed version bump type.
 */