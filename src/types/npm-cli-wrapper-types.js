import * as commitlintTypes from './npm-types.js';

/**
 * The options to pass to `version()`.
 * 
 * @since ${NEXT_VERSION}
 * @typedef {Partial<commitlintTypes.VersionOptionTypes>} VersionOptions
 */